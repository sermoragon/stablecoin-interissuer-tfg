{
  "name": "stablecoin-interissuer-tfg",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run start:dev -w apps/api",
    "build": "npm run build -w apps/api",
    "test": "npm run test -w apps/api",
    "lint": "npm run lint -w apps/api"
  }
}